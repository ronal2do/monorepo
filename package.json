{
  "name": "@boilerplate/main",
  "version": "1.0.0",
  "scripts": {
    "ui:sb": "yarn workspace @boilerplate/ui storybook",
    "ui:build": "yarn workspace @boilerplate/ui build",
    "ui:sb-build": "yarn workspace @boilerplate/ui build-storybook",
    "web:dev": "yarn workspace @boilerplate/web dev",
    "web:start": "yarn workspace @boilerplate/web start",
    "web:build": "yarn workspace @boilerplate/web build",
    "electron:start": "yarn workspace @boilerplate/desktop start",
    "ios": "yarn workspace @boilerplate/mobile ios"
  },
  "private": true,
  "workspaces": {
    "packages": [
      "packages/*"
    ],
    "nohoist": [
      "**/react-native",
      "**/react-native-*"
    ]
  },
  "devDependencies": {
    "lerna": "^3.16.4"
  }
}
